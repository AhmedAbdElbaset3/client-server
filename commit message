fix: a bug in auto reply


when a user send amessage it take long time to auto reply 
fix this by write another script which is faster


If you use an issue tracker, put references to them at the bottom, like this:
Resolves: #123
See also: #456, #789
